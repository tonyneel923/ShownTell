import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  appContainer: {
    flex: 1,
    marginTop: 30,
    // justifyContent: 'center',
    // alignItems: 'center',
    backgroundColor: '#F5FCFF',
  }
});
